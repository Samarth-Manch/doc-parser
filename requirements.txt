# Core document parsing
python-docx>=1.1.0
lxml>=5.0.0

# LLM providers (optional - needed for AI rules extraction)
openai>=1.0.0
anthropic>=0.20.0

# HTTP requests (optional - falls back to urllib)
requests>=2.31.0

# Environment variable loading
python-dotenv>=1.0.0
